export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold">Sign in to LivePulse</h1>
        <p className="mt-2 text-gray-500">Host login — OAuth / Magic Link</p>
      </div>
    </div>
  );
}
